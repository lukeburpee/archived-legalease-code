import React, { Component, PropTypes } from 'react';

export default class Cases extends Component {
	render(){
		return (
			<p style={{position: 'fixed', top:200, left:200, bottom:'auto', right:'auto'}}>This is the Cases Page</p>
		);
	}
}