const defaultState = {
	user: null,
	initial: defaultInitialPage,
	routes: defaultRoutes
}

const defaultRoutes = [
	{
		props: {
			name: '',
			title: '',

			component:'',
		}
	}
];

const defaultInitialPage = '';
