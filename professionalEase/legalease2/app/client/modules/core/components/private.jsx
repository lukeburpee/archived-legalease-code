import React, { Component, PropTypes } from 'react';
import Page from './../../interface/components/page.jsx';

const Private = ({content}) => (
	<div className="main">
		{content}
	</div>
);

export default Private